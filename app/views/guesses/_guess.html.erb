<%= turbo_stream_from guess %>
<div class="row" id="<%= dom_id(guess) %>">
  <% guess.evaluations.map.with_index do |evaluation, i| %>
    <div class="tile" data-state="<%= evaluation %>" data-animation="idle">
      <%= guess.word[i] %>
    </div>
  <% end %>
</div>